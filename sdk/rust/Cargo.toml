[package]
name = "hausfold-scruff"
version = "1.3.4"
edition = "2021"
rust-version = "1.75"
description = "Rust SDK for scruff, the worktree-lifecycle substrate. Shells out to the scruff binary; async (tokio) so it drops into an axum/tonic backend or an orchestrator equally."
readme = "README.md"
license = "MIT"
repository = "https://github.com/hausfold/scruff"
keywords = ["scruff", "worktree", "git", "agents"]
categories = ["development-tools", "asynchronous"]

[lib]
name = "scruff"
path = "src/lib.rs"

[dependencies]
tokio = { version = "1", features = ["process", "io-util", "time", "rt", "sync", "macros"] }
tokio-stream = { version = "0.1", features = ["io-util"] }
async-stream = "0.3"
futures-core = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "process", "time", "io-util", "sync"] }
futures-util = "0.3"
